import { FormDiv, HeroForm, HeroTitle } from './styles'

const Hero = () => (
  <HeroForm>
    <FormDiv>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </FormDiv>
  </HeroForm>
)

export default Hero
